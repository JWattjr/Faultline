# Bootstrap with npm run deploy:frontend first; that command verifies and saves the actual HTTPS origin.
FIXTURE_BASE_URL=

# Optional Studio Net deployer key. If absent, scripts create a local key in .env.
# DEPLOYER_PRIVATE_KEY=
